{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/first-project-be/package.json",
                "localRoot": "${workspaceFolder}/first-project-be"
            }
        }
    ]
}